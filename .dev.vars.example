# Local-dev-only secrets template. Copy to `.dev.vars` for `wrangler dev`.
# .dev.vars is NEVER deployed — for production secrets, use `wrangler secret put`.
# See README.md for the canonical list and setup instructions.

ANTHROPIC_API_KEY=sk-ant-
# OPENAI_API_KEY=sk-
HERMES_GATEWAY_TOKEN=
SLACK_BOT_TOKEN=xoxb-
SLACK_APP_TOKEN=xapp-
